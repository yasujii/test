# このリポジトリで作業するときの決まりごと（@sakura_cyoukatsu 桜腸活）

ユーザーから途中でもらった指示は全部ここに足していく。迷ったら一番新しい指示を優先。

## 言葉・伝え方
- 返事・途中経過・ツールの説明文・コミットメッセージは全部日本語。英語は使わない。
- 作業中は、今なにをしているかを日本語でこまめに伝える。
- 説明は高校生にも分かるように、専門用語を避けて具体的に。

## ラッコキーワード
- 使ってよいのは、毎月付与されるクレジットの範囲だけ（追加購入はしない）。
- 毎回の許可確認は出さない（`.claude/settings.json` で常に許可済み）。
- 新しいコネクタ（MCP）の接続は、ユーザーが頼んだときだけ。

## データの集め方
- YouTube：環境「yasujii（安G）」の `GSC_SA_JSON`（Googleの認証情報）で公式APIを使う。個人で発信しているチャンネルだけ。コメントした人の名前やIDは保存しない。
- 知恵袋：robots.txtで許可されたカテゴリ一覧だけ（検索ページは使わない）。2.5秒以上あける。
- Threads：公式APIは読み取り専用で、自分の投稿の数字だけ。他アカウントの分析に **Claude in Chrome は使わない**（2026-09-25 ユーザー指示）。他の方法は `threads/40_他アカウント伸び投稿の分析.md` の調査結果に沿う。
- 2026-09-25 の実験（`threads-autopost/others_probe.py`）で確認済み：長期トークンでは他人の投稿は検索できず（547件中0件）、他人の投稿ごとの表示回数は項目自体が存在しない。コネクタ（MCP）も同じ窓口なので同じ。
- Threadsのトークンは GitHub の Secret `THREADS_ACCESS_TOKEN` に入れる。`.github/workflows/threads-token-refresh.yml` が毎週月曜に自動延長して書き換える（書き換え用に Secret `GH_SECRETS_PAT` が必要）。トークンの値はファイル・コマンド・返事に絶対に書かない。
- 集めた生データとデータベースは公開リポジトリに載せない（.gitignore済み）。ユーザーに直接渡す。

## Apify（他アカウントの分析）※2026-09-25 ユーザー指示
- 他アカウントの投稿数（投稿ペース）・表示回数・マネタイズできているかを調べるために使う。**毎月の無料枠（5ドル分）だけ**。有料プランや追加購入はしない。1回の作業で使うのは4ドルまでを目安にする。
- 接続は claude.ai のカスタムコネクタ「Apify」（URL `https://mcp.apify.com?tools=actors,runs,storage,docs,futurizerush/meta-threads-scraper`）。ログインはユーザー本人がブラウザで行う。
- 使う道具は `futurizerush/meta-threads-scraper`（ログイン不要）。**自分のThreadsアカウントでログインする道具は使わない。**
- Threadsの規約は自動収集を禁止している。ユーザーはそのリスクを理解したうえでApifyを選んだ（9/25）。
- 生データは `threads/research/raw/apify/` に保存し、集計は `threads/research/apify_others_analyze.py` で行う。個人の連絡先（メール・電話）は保存しない。
- 公開リポジトリに載せる分析（40番）では、他アカウントの名前は出さず「A・B・C…」で書く。名前入りの表はCSVでユーザーに直接渡す。
- 手順は `threads/40_他アカウント伸び投稿の分析.md` の7番。

## 投稿の本数と方針 ※2026-09-25 ユーザー指示
- **1日5本**。そのうち朝・昼・晩の3本は3スライドツリー（06:00・12:00・21:00）、残り2本は単発（08:00・18:00、質問で終わってコメントを集める）。
- バズ（1投稿50万〜100万回）も狙うが、**フォロワーの悩みに寄り添って解決する発信**を大事にする。商品ページ（楽天）へのつなぎは4枚目のルールどおり。
- 投稿は**ユーザーが手動で予約**する。コピペしやすい形（1枚ずつコードブロック）で渡す。
- 材料：YouTubeは**個人の発信者だけ**（企業・公式・テレビ・病院法人は除く）、登録1万人以上・コメント500件以上の腸の動画。海外（英語圏）も比べて、日本との違いを投稿に使う。コメント欄の実体験を材料にする（丸写しはしない）。
- 大バズの型は `threads/41_大バズの型とキーワード_9-25.md`。

## 投稿の書き方（3スライドツリー）
- 改行なし。全スライドひと続き。
- 1枚目：強いフック＋答えは絶対に出さない＋「、、、」で切る。2枚目も「、、、」で切る。答えは3枚目。
- エロ系のフックは隔日（週4本まで）。同じ書き出しを続けない（ちょっとエロい／エッチぃ／エッチな話なんだけど、で回す）。
- 時間は6:00が最強、次が21:00。12時台は弱い。
- 語り手は「消化器内科での母の話を記録する娘」。先生の話は「〜と言われています」「〜だそうです」で書く。
- 本文に「治る・効く・痩せる・死」を使わない（「必死」なども不可）。危ないサインは受診をすすめる。
- 白犬さんの内容はまねしない。

## 楽天アフィリエイト
- リンクは4枚目だけ。【PR】を付ける。投稿後1時間で1,000インプを超えた投稿だけ、週2本まで。
- コメント欄・コメント返信にはリンクを貼らない。売り込みなしと言われた相手には紹介しない。
- 医薬品（便秘薬・整腸剤・漢方など）は紹介しない。

## 触ってはいけないもの
- Typefullyの下書き・予約は、ユーザーが頼んだとき以外は触らない。
- GitHub Actionsの自動投稿は停止中のまま。勝手に再開しない。
- トークンやパスワードをチャットに貼ってもらわない。画面にも出さない。貼られても使わず、ファイル・コマンド・返事に書き写さない（変更をすすめる）。
