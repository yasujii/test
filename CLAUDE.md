# Project Rules

## 最重要：ファイル・フォルダの削除禁止

- 作業でファイルやフォルダを削除しない（`rm`／`rmdir`／`git rm`、中身を空にする上書きも禁止）。削除が必要そうならユーザーに確認する。

## トークン節約（全リポジトリ共通・2026-09-26）

> 詳しい理由・設定一覧・依頼テンプレートは `yasujii/take-blog-rules-shared` の `.agent/token-saving.md` が正本。

- **1チャット＝1つの仕事。** 無関係な作業は新しいチャットで。同じ仕事を続けるときだけ `/compact`（残す情報を指定）。
- **このファイルに長い手順を足さない。** 手順は別ファイルに書き、ここには1行の案内だけ置く（目安200行以内）。
- **大きいファイル・ログを丸読みしない。** 必要な箇所だけ検索して読む。スクリプトの出力は要約にし、全件はファイルへ。
- **モデル・effortは作業の最初に決めて途中で変えない。** 軽作業は `/model sonnet`。
- **定期チェック・ポーリングはしない**（ユーザーが頼んだときだけ）。
- 重い調査はサブエージェントに任せて結論だけ受け取る。小さな修正はメイン会話で直接やる。
- `.claude/settings.json` でコマンド出力・MCP出力・スキル説明の上限を設定済み（`bashOutputMaxChars`／`MAX_MCP_OUTPUT_TOKENS`／`skillListingMaxDescChars`）。
